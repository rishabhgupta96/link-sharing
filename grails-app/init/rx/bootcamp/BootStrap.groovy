package rx.bootcamp

class BootStrap {

    def init = { servletContext ->

        /* User u=new User(firstName:'rish',lastName: 'sample')
        //User u1=new User(firstName:'ris',lastName:'gup')
        Child cd=new Child(firstName: 'rishabh')
        Child cd1=new Child(firstName: 'ri')
        //cd.signup=u
        //cd1.signup=u
        cd.signup=u
        cd1.signup=u
        //u.child=u1
        u.save()
        cd.save()
        cd1.save()
        if(u.validate())
            u.save()
        else
        {
            println "cant save"
            u.errors.allErrors.each{
                println it
            }
        }*/


    }
    def destroy = {
    }

}
